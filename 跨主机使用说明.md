# 跨主机使用说明

## 问题原因

组播（Multicast）方式出现绿屏是因为：
- Windows防火墙可能阻止组播流量
- 网卡/交换机/路由器可能不支持IGMP组播协议
- 组播配置复杂，容易出错

## 推荐方案：客户端指定服务端IP

当前实现**已经支持跨主机**，只需客户端指定正确的IP即可。

### 网络拓扑示例

```
服务端（摄像头主机）: 192.168.1.100
├── 运行 sync_camera.exe
└── UDP端口: 5010, 5011

客户端1: 192.168.1.101
客户端2: 192.168.1.102  
客户端3: 192.168.1.103
└── 都可以连接到服务端接收视频
```

---

## 使用方法

### 1. 服务端配置（摄像头主机）

**无需任何修改**，直接运行：

```bash
cd build
run_sync_camera.bat
```

服务端会在所有网卡上监听UDP端口5010和5011。

### 2. 客户端配置

#### 方式A：Python客户端

```python
from usbCamera import Camera

# 指定服务端IP地址
camera = Camera(host='192.168.1.100', start_port=5010)
camera.open()

# 读取帧
frames = camera.read_all()
```

#### 方式B：ffplay播放（本机测试）

本机测试脚本（`test_udp.bat`）：
```batch
ffplay -i udp://127.0.0.1:5010
ffplay -i udp://127.0.0.1:5011
```

#### 方式C：ffplay播放（跨主机）

其他主机上创建脚本：

**test_remote.bat**
```batch
@echo off
set SERVER_IP=192.168.1.100

start "Camera-5010" ffplay -fflags nobuffer -flags low_delay -probesize 32 -analyzeduration 0 -sync ext -framedrop -f mpegts -i udp://%SERVER_IP%:5010 -x 640 -y 480

timeout /t 1 /nobreak >nul

start "Camera-5011" ffplay -fflags nobuffer -flags low_delay -probesize 32 -analyzeduration 0 -sync ext -framedrop -f mpegts -i udp://%SERVER_IP%:5011 -x 640 -y 480
```

---

## 防火墙配置

### Windows防火墙（服务端）

需要允许UDP端口出站：

```powershell
# 允许5010和5011端口UDP出站
netsh advfirewall firewall add rule name="CameraStream-5010" dir=out action=allow protocol=UDP localport=5010
netsh advfirewall firewall add rule name="CameraStream-5011" dir=out action=allow protocol=UDP localport=5011
```

或者在Windows Defender防火墙界面：
1. 控制面板 → 系统和安全 → Windows Defender 防火墙
2. 高级设置 → 出站规则 → 新建规则
3. 选择"端口" → UDP → 特定本地端口：5010,5011
4. 允许连接 → 完成

---

## 验证连接

### 1. 查看服务端IP

服务端主机执行：
```bash
ipconfig
```
找到局域网IP地址（如：192.168.1.100）

### 2. 测试网络连通性

客户端主机执行：
```bash
ping 192.168.1.100
```
确保能ping通

### 3. 测试UDP接收

客户端执行：
```bash
# 替换为实际服务端IP
ffplay -i udp://192.168.1.100:5010
```

---

## 常见问题

### Q1: 客户端收不到画面
**检查项：**
- 服务端sync_camera.exe是否正在运行
- 防火墙是否允许UDP端口5010/5011
- 客户端IP是否填写正确
- 网络是否连通（ping测试）

### Q2: 延迟很大
**解决方案：**
- 确保局域网环境良好（千兆交换机）
- 减少网络跳数（避免通过多个路由器）
- 使用有线连接而非WiFi

### Q3: 多个客户端能否同时接收？
**可以！** UDP协议支持多个客户端同时监听同一个端口的流。每个客户端独立接收数据包。

---

## 性能优化

### 局域网推荐配置
- 使用千兆以太网
- 服务端和客户端直连同一交换机
- 避免WiFi（延迟和丢包率高）

### 带宽计算
- 分辨率：640x480
- 帧率：30fps
- 比特率：4Mbps（每路）
- 双摄像头：约8Mbps总带宽

千兆网络完全够用。

---

## 总结

✅ **当前方案优势：**
- 配置简单，只需客户端指定IP
- 支持多客户端同时接收
- 不依赖组播协议
- 防火墙配置明确

❌ **组播方案问题：**
- 配置复杂
- 兼容性差
- 防火墙容易阻止
- 调试困难

**建议：使用客户端指定服务端IP的方案，简单可靠！**
